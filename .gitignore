/.quarto/
.Rproj.user

**/*.quarto_ipynb

# Data files (generated by prepare-data scripts)
dashboard/vigitel/vigitel_aggregated.rds

# Data prep cache and logs
data-prep/cache/
data-prep/*.log

# gdfpd2 data cache (large zip/rds files)
**/gdfpd2_cache/

# Claude Code local settings
.claude/

# Saida gerada. O GitHub Pages publica do branch `gh-pages` (conferido em
# 31/08/2026 via API), entao `_site/` no main nao serve para nada e sao ~8 MB
# que mudam por completo a cada render, enterrando o diff do que importa.
# `.quarto/` e cache de build. Os dois estavam rastreados por inercia.
_site/
.quarto/

# Cache do wrangler (deploy do site). Contem o id da conta; nao e fonte.
.wrangler/
